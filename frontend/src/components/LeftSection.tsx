import * as React from 'react';

const LeftSection: React.FunctionComponent = () => {
  return (
    <section className='w-[5vw] bg-gray-200'>
        Left Section
    </section>
  );
};

export default LeftSection;
