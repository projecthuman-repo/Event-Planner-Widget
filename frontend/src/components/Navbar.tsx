import * as React from 'react';

const Navbar: React.FunctionComponent = () => {
  return (
    <section className='w-[100%] h-[10%] bg-gray-200'>
        Navbar
    </section>
  );
};

export default Navbar;